[project]
name = "hospital-capacity-scheduled-retraining"
version = "0.1.0"
description = "Airflow DAG for scheduled ML retraining with evaluation and gated auto-promotion"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0",
  "numpy>=1.24",
  "scikit-learn>=1.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
